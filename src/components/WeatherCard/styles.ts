import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  card: {
    width: "30%",
  },
  mobile: {
    width: "100%",
    marginBottom: "1rem",
  },
  text: {
    marginBottom: 12,
  },
  media: {
    maxWidth: "100px",
    height: "auto",
    marginTop: "-20px",
  },
});

export default useStyles;
